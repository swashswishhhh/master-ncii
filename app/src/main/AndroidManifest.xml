<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/avd_robot_mascot"
        android:label="@string/app_name"
        android:roundIcon="@drawable/avd_robot_mascot"
        android:supportsRtl="true"
        android:theme="@style/Theme.ServerMasterNCII">

        <!-- ══ SPLASH — entry point ══════════════════════════════ -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ══ AUTH ══════════════════════════════════════════════ -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ══ MAIN GAME ══════════════════════════════════════════ -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".QuizActivity"
            android:exported="false" />
        <activity
            android:name=".ResultActivity"
            android:exported="false" />

        <!-- ══ SIMULATORS ════════════════════════════════════════ -->
        <activity
            android:name=".WorkgroupConfigActivity"
            android:exported="false" />
        <activity
            android:name=".FirewallSimulatorActivity"
            android:exported="false" />
        <activity
            android:name=".IPConfigSimulatorActivity"
            android:exported="false" />
        <activity
            android:name=".TerminalEmulatorActivity"
            android:exported="false" />
        <activity
            android:name=".StepSequencerActivity"
            android:exported="false" />
        <activity
            android:name=".PermissionSimulatorActivity"
            android:exported="false" />
        <activity
            android:name=".ServerMonitorActivity"
            android:exported="false" />
        <activity
            android:name=".DecisionTreeActivity"
            android:exported="false" />



        <!-- ══ PROFILE / SETTINGS ════════════════════════════════ -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Settings"
            android:theme="@style/Theme.ServerMasterNCII" />

        <!-- ══ ADMIN ══════════════════════════════════════════════ -->
        <activity
            android:name=".admin.AdminDashboardActivity"
            android:exported="false"
            android:label="Admin Console"
            android:theme="@style/Theme.ServerMasterNCII" />
        <activity
            android:name=".admin.MissionManagerActivity"
            android:exported="false" />
        <activity
            android:name=".admin.AddMissionActivity"
            android:exported="false"
            android:label="Add Mission" />
        <activity
            android:name=".admin.QuestionManagerActivity"
            android:exported="false"
            android:label="Question Manager"
            android:theme="@style/Theme.ServerMasterNCII" />
        <activity
            android:name=".admin.AddQuestionActivity"
            android:exported="false"
            android:label="Add Question"
            android:theme="@style/Theme.ServerMasterNCII" />

        <activity
            android:name=".admin.AnalyticsActivity"
            android:exported="false"/>

        <activity
            android:name=".admin.PlayerDetailActivity"
            android:exported="false"/>



        <!-- ══ FACEBOOK SDK ═══════════════════════════════════════ -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:exported="true" />

        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <activity
            android:name=".admin.ChapterMissionsActivity"
            android:exported="false"/>

    </application>
</manifest>